"""Policy-compatible WFMHub 2 portable host.

This package deliberately imports only the Python standard library. Browser
WebAssembly capabilities are optional probes; SQLite remains the durable host
boundary already accepted on the target workstation.
"""

__version__ = "0.2.0-phase1-source-preview.9"
